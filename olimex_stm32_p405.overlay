/* olimex_stm32_p405.overlay */

/ {
    aliases {
        /* Route the virtual USB shell to an ISOLATED UART */
        cdc-acm-uart0 = &usart1; 
    };
};

/* CRITICAL FIX: Turn off the physical USB controller to free up the PA9 pin */
&usbotg_fs {
    status = "disabled";
};

/* Enable the isolated UART so the Shell compiles and boots safely */
&usart1 { 
    status = "okay"; 
    pinctrl-0 = <&usart1_tx_pa9 &usart1_rx_pa10>;
    pinctrl-names = "default";
};

/* Ensure standard UART is enabled for the OS Logger */
&usart2 { 
    status = "okay"; 
};

/* Enable physical hardware endpoints so Zephyr instantiates the real drivers */
&i2c1 { 
    status = "okay"; 
    pinctrl-0 = <&i2c1_scl_pb6 &i2c1_sda_pb7>;
    pinctrl-names = "default";
};

&rtc { status = "okay"; };
&iwdg { status = "okay"; };

/* Define a 128KB NVS storage partition at the end of the 1MB Flash */
&flash0 {
    partitions {
        compatible = "fixed-partitions";
        #address-cells = <1>;
        #size-cells = <1>;

        storage_partition: partition@E0000 {
            label = "storage";
            reg = <0x000E0000 0x00020000>;
        };
    };
};
