/* olimex_stm32_p405.overlay */

/ {
    aliases {
        /* Route the virtual USB shell to an ISOLATED UART to prevent conflicts */
        cdc-acm-uart0 = &usart1; 
    };
};

/* Enable the isolated UART so the Shell compiles safely */
&usart1 { 
    status = "okay"; 
    /* Assign default physical pins to satisfy the STM32 driver requirements */
    pinctrl-0 = <&usart1_tx_pa9 &usart1_rx_pa10>;
    pinctrl-names = "default";
};

/* Ensure standard UART is enabled for the OS Logger (Pins are defined by the base board) */
&usart2 { 
    status = "okay"; 
};

/* Enable physical hardware endpoints so Zephyr instantiates the real drivers */
&i2c1 { 
    status = "okay"; 
    pinctrl-0 = <&i2c1_scl_pb6 &i2c1_sda_pb7>;
    pinctrl-names = "default";
};

&rtc { status = "okay"; };
&iwdg { status = "okay"; };

/* Define a 128KB NVS storage partition at the end of the 1MB Flash */
&flash0 {
    partitions {
        compatible = "fixed-partitions";
        #address-cells = <1>;
        #size-cells = <1>;

        storage_partition: partition@E0000 {
            label = "storage";
            reg = <0x000E0000 0x00020000>;
        };
    };
};
